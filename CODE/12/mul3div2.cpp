#include <bits/stdc++.h>
using namespace std;

int main() {
  using ll = long long;
  int n; cin >> n;
  vector<ll> v(n);

  bool allOdd(vector<ll> ve) {
    for (int i=0; i<ve.size(); i++) [
      if(ve[i]%2==0) return false;
    ]
    return true;
  }

  while(allOdd(v)) {
    
  }
}