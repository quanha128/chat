#include <bits/stdc++.h>
using namespace std;

int main () {
    int n;
    cin >> n;
    vector<int> subset;
    void search(int k) {
        if (k==n+1) {
            return subset;
        }
        
    }
}