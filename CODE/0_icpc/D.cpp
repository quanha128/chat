#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    while (cin >> n, n>0)
    {
        vector<int> v(n);

        for (int i=0; i<n; i++) {
            int in;
            cin >> in;
            v[i] = in;
        }

        
    }
    return 0;
}